const axios = require('axios');
const dotenv = require('dotenv').config();

module.exports = {
	getTeams: (req, res) => {
		try {
		} catch (error) {}
	},
	getTeamById: (req, res) => {
		try {
		} catch (error) {}
	},
};
